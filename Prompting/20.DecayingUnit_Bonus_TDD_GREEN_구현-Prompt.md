# 대화형 프롬프트 Export — UnitConverter_12 (DecayingUnit Bonus TDD GREEN 구현)

| 항목 | 내용 |
|------|------|
| **대응 보고서** | [Report/20.DecayingUnit_Bonus_TDD_GREEN_구현.md](../Report/20.DecayingUnit_Bonus_TDD_GREEN_구현.md) |
| **선행 Prompt Export** | [19.DecayingUnit_Bonus_RED_테스트작성-Prompt.md](./19.DecayingUnit_Bonus_RED_테스트작성-Prompt.md) (Bonus RED BT-01~06 · 스텁) |
| **Export 일자** | 2026-05-21 |
| **워크스페이스** | `c:\DEV\UnitConverter_12` |
| **형식** | Turn 기반 대화형 transcript (User → Assistant 산출 요약) |

---

## 사용 안내

- 각 **Turn**의 `User` 블록은 실제 입력 프롬프트 원문이다.
- `Assistant` 블록은 해당 턴에서 생성된 **산출물 요약**이다 (전체 응답 본문 아님).
- Turn 1: **DecayingUnit Bonus GREEN** — OCP `UnitRegistry` · BT-01~06 PASS · 회귀 확인 · 커밋.
- Turn 2: **진행 보고서** — `Report/20.DecayingUnit_Bonus_TDD_GREEN_구현.md`.
- Turn 3: 본 Export (문서만).
- 재현 시 Turn 1→2 순서 권장. Turn 1 단독으로 Bonus GREEN만 재현 가능.
- **선행 전제**: RPT-19 Bonus RED(BT-01~06 FAIL) · BCE Domain GREEN · `UnitConverter.cpp` 미수정.

---

## Turn 1 — DecayingUnit Bonus TDD GREEN (OCP UnitRegistry)

### User

```
[단계] TDD GREEN만 수행. REFACTOR는 하지 않는다.
프로젝트: @c:\DEV\UnitConverter_12\ 

구현 접근법: OCP: 기존 if-else 수정 없이 UnitRegistry에 새 항목만 등록

할 일:
1. cmake --build build && ctest --test-dir build 실행 → BT-01~06 FAIL 확인
2. registerUnit() 메서드/함수 최소 구현:
   - 내부 UnitRegistry(Map/Dict)에 단위명·비율 저장
   - convert()는 Registry를 조회하여 meter 허브 경유 변환
   - Item 구조체(UnitConverter.cpp의 기존 변환 로직) 수정 금지
3. cmake --build build && ctest --test-dir build 재실행 → BT-01~06 PASS 확인
4. 기존 회귀 테스트 전체 PASS 확인:
   cmake -S . -B build && cmake --build build

산출물:
- BT-01~06이 통과하는 최소 구현 코드
- 기존 TC-A/TC-B 전체 PASS 유지 확인
- 커밋 메시지: feat(feature): add registerUnit with OCP registry
```

### Assistant (산출 요약)

- **RED 확인**:
  ```powershell
  cmake --build build
  .\build\Debug\unit_converter_bonus_tests.exe
  # test cases: 1 failed | assertions: 6 failed (의도적 RED)
  ```
- **수정 파일**: [src/DecayingUnit.cpp](../src/DecayingUnit.cpp) **only**
  - OCP: 기존 `vector<Item> units`(meter/feet/yard) **미변경**
  - 확장: `Impl::registry` (`std::map<std::string, double>`) — `registerUnit` 전용
  - `lookupMetersPerUnit(registry, units, name)` — registry → units 순 조회
  - `registerUnit`: `ratio > 0` else `std::invalid_argument`; `registry[name] = k`
  - `convert`: meter 허브 `value × (k_from / k_to)`
  - `convertAll`: `units` + `registry` 순회, source 제외
- **GREEN 확인**:
  ```powershell
  .\build\Debug\unit_converter_bonus_tests.exe
  # All tests passed (10 assertions in 1 test case)
  ```
- **회귀**:
  - TC-A (#36~41): ✅ PASS
  - TC-B (#43~49): ✅ PASS
  - BCE Domain/Data · Golden Master: ✅ PASS
  - `json_format_returns_valid_schema` (#42): ❌ RED 유지 (의도적)
  - ctest #50 한글 TEST_CASE명: ⚠️ 필터 인코딩 — exe 직접 실행 시 PASS
- **커밋**: `398ef10` — `feat(feature): add registerUnit with OCP registry`
- **미변경**: `DecayingUnit.hpp`, `test_decaying_unit.cpp`, `UnitConverter.cpp`, `src/domain/*`, REFACTOR

---

## Turn 2 — 진행 보고서 작성

### User

```
Report폴더에 현재 진행한 내용을 바탕으로 새로운 보고서를 작성해줘.
```

### Assistant (산출 요약)

- [19.DecayingUnit_Bonus_RED_테스트작성.md](../Report/19.DecayingUnit_Bonus_RED_테스트작성.md) · [12.DualTrack_TDD_GREEN_증분구현.md](../Report/12.DualTrack_TDD_GREEN_증분구현.md) 보고 형식 준수
- **RPT-2026-05-21-20**, 선행 RPT-19, OCP Registry GREEN · BT-01~06 · 회귀 · 커밋 `398ef10`
- §1~13: Executive Summary, OCP 설계, BT 결과표, ctest 회귀, BCE vs Bonus, RED→GREEN 전환, Traceability
- **산출 파일**: `Report/20.DecayingUnit_Bonus_TDD_GREEN_구현.md`

---

## Turn 3 — 본 Export (현재 턴)

### User

```
현재까지의 프롬프트를 대화형 프롬프트로 Prompting 폴더에 Export transcript 해줘.
파일명규칙: (Report보고서 파일명)-Prompt.md
```

### Assistant (산출 요약)

- 본 문서 생성
- **산출 파일**: `Prompting/20.DecayingUnit_Bonus_TDD_GREEN_구현-Prompt.md`

---

## 산출물 매핑 (Turn → 파일)

| Turn | 작업 | 저장소 파일 |
|------|------|-------------|
| 1 | Bonus GREEN OCP Registry | `src/DecayingUnit.cpp` |
| 1 | Git 커밋 | `398ef10 feat(feature): add registerUnit with OCP registry` |
| 2 | 진행 보고서 | `Report/20.DecayingUnit_Bonus_TDD_GREEN_구현.md` |
| 3 | Prompt Export | `Prompting/20.DecayingUnit_Bonus_TDD_GREEN_구현-Prompt.md` |

**의도적 미변경**: `src/DecayingUnit.hpp`, `tests/test_decaying_unit.cpp`, `UnitConverter.cpp`, `src/domain/*`, `docs/defect_list.md`, REFACTOR.

**선행 세션**: Bonus RED ([19.DecayingUnit_Bonus_RED_테스트작성-Prompt.md](./19.DecayingUnit_Bonus_RED_테스트작성-Prompt.md)) · BCE Domain cubit GREEN ([06.Catch2_TDD_GREEN_구현-Prompt.md](./06.Catch2_TDD_GREEN_구현-Prompt.md)).

---

## Phase 재현용 — 축약 연속 프롬프트 (복사용)

> 새 세션에서 Turn 1~2를 한 번에 재현할 때:

```
워크스페이스: c:\DEV\UnitConverter_12
전제: RPT-19 Bonus RED 완료 (BT-01~06 FAIL, DecayingUnit 스텁).

[Turn 1] @src/DecayingUnit.cpp @tests/test_decaying_unit.cpp
TDD GREEN only. REFACTOR 금지.
OCP: Item vector(meter/feet/yard) 미변경, registry Map에 registerUnit만.
convert: meter 허브 value*(k_from/k_to). convertAll: source 제외.
Step: cmake --build → RED 6/6 FAIL → GREEN 구현 → bonus_tests PASS → ctest 회귀.
커밋: feat(feature): add registerUnit with OCP registry
금지: UnitConverter.cpp, src/domain/*, test 수정.

[Turn 2] Report/20.DecayingUnit_Bonus_TDD_GREEN_구현.md 작성.
19·12 보고 형식, OCP·BT 결과·회귀·398ef10 포함.
```

---

## Turn 1 단독 재현용 — Bonus GREEN 전체

```text
@c:\DEV\UnitConverter_12\ @src/DecayingUnit.cpp @tests/test_decaying_unit.cpp @Report/19.DecayingUnit_Bonus_RED_테스트작성.md

[단계] TDD GREEN만. REFACTOR 금지.
구현 접근: OCP — 기존 Item vector 수정 없이 registry Map에만 동적 등록.

할 일:
1. cmake --build build && bonus_tests.exe → BT-01~06 FAIL 확인
2. registerUnit: Map 저장, ratio>0 else std::invalid_argument
3. convert: registry/units 조회 + meter 허브
4. convertAll: source 제외 units+registry 순회
5. bonus_tests 6/6 PASS, TC-A/TC-B 회귀 PASS
6. git commit -m "feat(feature): add registerUnit with OCP registry"

금지: DecayingUnit.hpp, test_decaying_unit.cpp, UnitConverter.cpp, src/domain/*, REFACTOR
```

---

## Turn 2 연계 — REFACTOR 단계 (20 §10, 선택)

[20.DecayingUnit_Bonus_TDD_GREEN_구현.md](../Report/20.DecayingUnit_Bonus_TDD_GREEN_구현.md) §10 기준:

```text
@src/DecayingUnit.cpp @Report/20.DecayingUnit_Bonus_TDD_GREEN_구현.md

REFACTOR only — Bonus BT-01~06 PASS 유지.
Item vector + registry Map → 단일 UnitRegistry 통합 (선택).
중복 등록 정책·미등록 단위 예외 처리 검토.
금지: BCE Domain, UnitConverter.cpp
```

---

## 주의 (프롬프트 해석)

| 항목 | User 요청 | Assistant·문서 처리 |
|------|-----------|---------------------|
| OCP | UnitRegistry에 새 항목만 | `vector<Item>` 고정 + `map registry` 확장 |
| Item 수정 금지 | UnitConverter.cpp | `Item`은 `DecayingUnit.cpp` 내부 — **구조·초기값 불변** |
| if-else 수정 없이 | OCP | 기본 3단위는 vector 유지, 동적만 Map |
| BT-04 | `std::invalid_argument` | Domain `InvalidMetersPerUnitError`와 **별도** |
| ctest | `--test-dir build` | MSVC는 `-C Debug` 필요 · #50 한글명 exe 직접 실행 |
| REFACTOR | 금지 | Item/registry 통합·lookup 예외는 **후속** |
| 응답 언어 | user rule | 한국어 |

---

## Bonus TC 빠른 참조 (GREEN 기대값)

| Test ID | When | Then (±1e-5) | GREEN 구현 |
|---------|------|--------------|------------|
| BT-01 | cubit→meter | 0.4572 | registry lookup + 허브 |
| BT-02 | meter→cubit | 1/0.4572 | registry lookup + 허브 |
| BT-03 | cubit→feet | 0.4572×3.28084 | registry + units 허브 |
| BT-04 | registerUnit(-0.4572) | invalid_argument | ratio 검증 |
| BT-05 | convertAll(cubit,1) | size=3 | units 순회, cubit 제외 |
| BT-06 | meter→feet | 3.28084 | units 기본값 회귀 |

---

## RED / GREEN 검증 명령 (Windows MSVC)

```powershell
cmake -S . -B build
cmake --build build

# RED (구현 전)
.\build\Debug\unit_converter_bonus_tests.exe
# 기대: test cases 1 failed | assertions 6 failed

# GREEN (구현 후)
.\build\Debug\unit_converter_bonus_tests.exe
# 기대: All tests passed (10 assertions in 1 test case)

# 회귀
cmake -S . -B build; cmake --build build
ctest --test-dir build -C Debug --output-on-failure
# 기대: 49/51 (json RED 1 + ctest #50 인코딩 1)
```

---

## OCP Impl 구조 (GREEN 재현 참고)

```cpp
struct DecayingUnit::Impl {
    std::vector<Item> units{
        {"meter", 1.0},
        {"feet", 0.3048},
        {"yard", 0.9144},
    };
    std::map<std::string, double> registry;  // registerUnit only
};

// convert: value * (k_from / k_to)
// lookup: registry → units
```

---

## BCE Domain vs Bonus (GREEN 후)

| | BCE `UnitCatalog` | Bonus `DecayingUnit` |
|--|-------------------|----------------------|
| 테스트 | `domain_register_tests.cpp` `[domain][register]` | `test_decaying_unit.cpp` `[bonus]` |
| 저장 | `std::map` 단일 | `vector<Item>` + `map registry` |
| 음수 k | `InvalidMetersPerUnitError` | `std::invalid_argument` |
| GREEN 상태 | ✅ 완료 | ✅ **본 세션** |

---

## Git 커밋 (Turn 1)

| 해시 | 메시지 |
|------|--------|
| `398ef10` | `feat(feature): add registerUnit with OCP registry` |

**diff**: `src/DecayingUnit.cpp` — 37 insertions, 10 deletions

---

## 문서 이력

| 버전 | 일자 | 변경 |
|------|------|------|
| 1.0 | 2026-05-21 | 최초 Export (Turn 1~3, 대응 보고서 `20.DecayingUnit_Bonus_TDD_GREEN_구현.md`) |
