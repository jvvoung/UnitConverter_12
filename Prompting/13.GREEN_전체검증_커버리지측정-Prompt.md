# 대화형 프롬프트 Export — UnitConverter_12 (GREEN 전체 검증·커버리지 측정)

| 항목 | 내용 |
|------|------|
| **대응 보고서** | [Report/13.GREEN_전체검증_커버리지측정.md](../Report/13.GREEN_전체검증_커버리지측정.md) |
| **선행 Prompt Export** | [12.DualTrack_TDD_GREEN_증분구현-Prompt.md](./12.DualTrack_TDD_GREEN_증분구현-Prompt.md) (9 커밋 GREEN) |
| **Export 일자** | 2026-05-21 |
| **워크스페이스** | `c:\DEV\UnitConverter_12` |
| **형식** | Turn 기반 대화형 transcript (User → Assistant 산출 요약) |

---

## 사용 안내

- 각 **Turn**의 `User` 블록은 실제 입력 프롬프트 원문이다.
- `Assistant` 블록은 해당 턴에서 생성된 **산출물 요약**이다 (전체 응답 본문 아님).
- Turn 1: **전체 ctest + gcov/lcov 커버리지 + 5항목 확인 체크리스트**.
- Turn 2: **진행 보고서** — `Report/13.GREEN_전체검증_커버리지측정.md`.
- Turn 3: 본 Export (문서만).
- 재현 시 Turn 1→2 순서 권장.
- **선행 전제**: RPT-12 Dual-Track 9 커밋 GREEN 완료 · ctest 48/49 · BCE 35/35 GREEN.

---

## 5항목 확인 체크리스트 (세션 공통 기준)

| # | 확인 항목 | 목표 |
|---|----------|------|
| 1 | 모든 TC PASS (0 failures) | Dual-Track TC-A-01~07(6건) + TC-B-01~07(7건) |
| 2 | Domain Logic 커버리지 | Line ≥ 95% (`lcov --extract '*/src/domain/*'`) |
| 3 | Boundary Layer 커버리지 | Line ≥ 85% (`lcov --extract '*/src/boundary/*'`) |
| 4 | 비율 상수 인라인 금지 | `3.28084` / `1.09361` 리터럴 없음 (`src/` + CLI) |
| 5 | `main()` Domain 분리 | `UnitConverter.cpp`에 변환 로직 없음 |

**공통 Step**: (1) MSVC ctest 전체 실행 → (2) Dual-Track TC 필터 PASS 확인 → (3) MinGW `build_cov` + lcov 측정 → (4) 5항목 체크리스트 → (5) 보고서 작성

---

## Turn 1 — 전체 테스트·커버리지 검증

### User

```
모든 TC(TC-A-01~07, TC-B-01~07)가 통과한 후 전체 테스트를 실행하라.

cmake -S . -B build && cmake --build build ctest --test-dir build -V

커버리지 측정:
# gcov / lcov gcov UnitConverter.cpp

확인 항목:
- 모든 TC PASS (0 failures)
- Domain Logic 커버리지 ≥ 95%
- Boundary Layer 커버리지 ≥ 85%
- 비율 상수 3.28084 / 1.09361이 인라인으로 남아 있지 않음
- main() 함수에 변환 로직이 남아 있지 않음 (Domain으로 분리됨)
```

### Assistant (산출 요약)

**MSVC ctest 전체 실행**:

```powershell
cmake -S . -B build; cmake --build build; ctest --test-dir build -C Debug -V
```

| 구분 | 결과 |
|------|------|
| 전체 ctest | **48/49 PASS** (1 failure) |
| Dual-Track TC (A/B) | **13/13 PASS** (0 failures) |
| BCE 회귀 | **35/35 PASS** |
| RED 잔존 | `json_format_returns_valid_schema` (#42) — `FAIL("RED")` |

**Dual-Track TC 상세**: TC-A-01~04, 06~07 (6건) + TC-B-01~07 (7건) 전부 PASS. TC-A-05 미작성.

**MinGW 커버리지 측정** (`build_cov`, `ENABLE_COVERAGE=ON`):

- MinGW 빌드 시 `InputParser.cpp` `std::isfinite` 오류 → `#include <cmath>` 1줄 추가 (이식성 수정)
- lcov Domain: **91.9%** (68/74) — 목표 95% **미달**
- lcov Boundary: **81.4%** (79/97) — 목표 85% **미달**
- Boundary 병목: `TableFormatter.cpp` **22.2%**
- Branch: MinGW gcov `no data found`

**레거시 `UnitConverter.cpp` gcov** (CLI `meter:2.5` 1회):

- Line 64.71% · Branch 69.05% executed
- feet/yard 역변환·에러 분기 미커버

**5항목 체크리스트**:

| # | 항목 | 결과 |
|---|------|------|
| 1 | TC PASS | **부분** — Dual-Track 13/13 ✅ · 전체 48/49 (json RED) |
| 2 | Domain ≥ 95% | **미달** — 91.9% |
| 3 | Boundary ≥ 85% | **미달** — 81.4% |
| 4 | 인라인 상수 없음 | **미충족** — `UnitConverter.cpp` L35·37·44·45 |
| 5 | main() Domain 분리 | **미충족** — CLI 레거시 if-else + 인라인 환산 |

**비율 상수 위치**:

- ❌ `UnitConverter.cpp` — 리터럴 4곳
- ✅ `src/domain/UnitCatalog.cpp` — k=0.3048/0.9144
- ✅ `src/logic/UnitConverterLogic.cpp` · `ConversionApplicationService.cpp` — Engine 위임, 리터럴 없음
- ⚠️ `src/logic/conversion_ratios.hpp` — 정의만, 7번 커밋 이후 미참조

**PowerShell**: `&&` → **`;`** 구분 (Windows) · MSVC ctest `-C Debug` 필수

---

## Turn 2 — 진행 보고서 작성

### User

```
Report폴더에 현재 진행한 내용을 바탕으로 새로운 보고서를 작성해줘.
```

### Assistant (산출 요약)

- [12.DualTrack_TDD_GREEN_증분구현.md](../Report/12.DualTrack_TDD_GREEN_증분구현.md) 보고 형식 준수
- **RPT-2026-05-21-13**, 선행 RPT-12, GREEN 완료 후 **검증·측정 세션**
- §1~13: Executive Summary, 검증 절차(MSVC+MinGW), ctest 48/49, Dual-Track 13/13, lcov Domain 91.9%/Boundary 81.4%, 5항목 체크리스트, 인라인 상수·CLI 갭, RPT-12 대비, 갭 분석, 권장 7단계
- **부수 변경**: `InputParser.cpp` `#include <cmath>` (MinGW 이식성)
- **산출 파일**: `Report/13.GREEN_전체검증_커버리지측정.md`

---

## Turn 3 — 본 Export (현재 턴)

### User

```
현재까지의 프롬프트를 대화형 프롬프트로 Prompting 폴더에 Export transcript 해줘.
파일명규칙: (Report보고서 파일명)-Prompt.md
```

### Assistant (산출 요약)

- 본 문서 생성
- **산출 파일**: `Prompting/13.GREEN_전체검증_커버리지측정-Prompt.md`

---

## 산출물 매핑 (Turn → 파일)

| Turn | 작업 | 저장소 파일 |
|------|------|-------------|
| 1 | ctest + lcov + 5항목 체크리스트 | `build/` (MSVC), `build_cov/` (MinGW), `build_cov/domain.info`, `build_cov/boundary.info` |
| 1 | MinGW 이식성 수정 | `src/boundary/InputParser.cpp` (`#include <cmath>`) |
| 2 | 진행 보고서 | `Report/13.GREEN_전체검증_커버리지측정.md` |
| 3 | Prompt Export | `Prompting/13.GREEN_전체검증_커버리지측정-Prompt.md` |

**의도적 미변경**: `UnitConverter.cpp` (CLI 레거시), `docs/defect_list.md`, README [x], `json_format_returns_valid_schema` (RED).

**선행 세션 산출** ([12.DualTrack_TDD_GREEN_증분구현-Prompt.md](./12.DualTrack_TDD_GREEN_증분구현-Prompt.md)): 9 커밋 GREEN · ctest 48/49 · Dual-Track 13/14 GREEN.

---

## Phase 재현용 — 축약 연속 프롬프트 (복사용)

> 새 세션에서 Turn 1~2를 한 번에 재현할 때:

```
워크스페이스: c:\DEV\UnitConverter_12
전제: RPT-12 GREEN 완료 — 9 커밋, ctest 48/49, Dual-Track 13/14 GREEN, BCE 35/35.

[Turn 1] GREEN 전체 검증·커버리지 측정
Step 1: MSVC ctest 전체
  cmake -S . -B build; cmake --build build; ctest --test-dir build -C Debug -V
  기대: 48/49 (json RED 1건), Dual-Track 13/13 PASS

Step 2: Dual-Track TC 필터
  ctest --test-dir build -C Debug -R "green|valid_input|...|load_config" --output-on-failure
  기대: 13/13 Passed

Step 3: MinGW 커버리지 (MSVC gcov 미지원)
  $env:Path = "C:\mingw64\bin;C:\ProgramData\chocolatey\bin;" + $env:Path
  cmake -S . -B build_cov -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug -DENABLE_COVERAGE=ON
  cmake --build build_cov -j 4
  ctest --test-dir build_cov --output-on-failure
  lcov --capture → extract domain/boundary → --summary

Step 4: 5항목 체크리스트
  1) Dual-Track 13/13 PASS
  2) Domain lcov ≥ 95% (현재 ~91.9% 미달)
  3) Boundary lcov ≥ 85% (현재 ~81.4% 미달)
  4) 3.28084/1.09361 인라인 없음 (UnitConverter.cpp 4곳 잔존)
  5) main() Domain 분리 (CLI 레거시)

Step 5: UnitConverter.cpp gcov (레거시 baseline)
  echo "meter:2.5" | .\build_cov\unit_converter_cli.exe
  gcov -b -o CMakeFiles/unit_converter_cli.dir ...

[Turn 2] Report/13.GREEN_전체검증_커버리지측정.md 작성.
12 보고 형식, ctest, lcov, 5항목, RPT-12 대비, M2 권장.
```

---

## Turn 1 단독 재현용 — ctest + 커버리지

```text
@c:\DEV\UnitConverter_12\ @docs/test_plan.md §6·§7 @Report/12.DualTrack_TDD_GREEN_증분구현.md

전제: RPT-12 GREEN 완료 (9 커밋, ctest 48/49).

[검증] Dual-Track TC-A-01~07(6건) + TC-B-01~07(7건) 전건 PASS 확인 후:

1. MSVC 전체 ctest
   cmake -S . -B build; cmake --build build; ctest --test-dir build -C Debug -V

2. MinGW lcov (ENABLE_COVERAGE=ON, build_cov)
   Domain ≥ 95%, Boundary ≥ 85%

3. 5항목 체크리스트:
   - TC PASS (0 failures)
   - Domain lcov ≥ 95%
   - Boundary lcov ≥ 85%
   - 3.28084/1.09361 인라인 없음
   - main() Domain 분리

4. gcov UnitConverter.cpp (레거시 baseline)

출력: ctest 표, lcov 요약, 5항목 PASS/FAIL 표, 미충족 항목 근거
```

---

## Turn 2 연계 — JSON GREEN (RPT-12 §13 #1)

[12.DualTrack_TDD_GREEN_증분구현.md](../Report/12.DualTrack_TDD_GREEN_증분구현.md) §13 기준:

```text
@c:\DEV\UnitConverter_12\ @tests/ui_boundary_red_tests.cpp @docs/PRD.md §6.3

TDD GREEN only — json_format_returns_valid_schema 단건.
ConversionApplicationService::convert(input, OutputFormat::Json)
FAIL("RED") → assertion 교체, [ui][green]
완료: ctest 49/49
커밋: feat(green): json format schema
```

---

## Turn 2 연계 — M2 CLI E2E (RPT-12 §13 #4)

```text
@c:\DEV\UnitConverter_12\ @UnitConverter.cpp @src/boundary/ConversionApplicationService.cpp @docs/defect_list.md

M2: ConversionApplicationService + CLI 연동.
DEF-002: meter:2.5 → 8.2 feet / 2.7 yard (1dp, source 행 제외)
DEF-003: meter:-2.5 → exit 1, POL-NEG
인라인 3.28084/1.09361 제거 → Catalog/Service 위임
GREEN: defect_list DEF-002·003 Closed · 5항목 #4·#5 충족
```

---

## Turn 2 연계 — 커버리지 게이트 충족

```text
@c:\DEV\UnitConverter_12\ @src/domain/ @src/boundary/ @docs/test_plan.md §7.4

커버리지 게이트 미달 해소:
- Domain 91.9% → ≥95%: UnitCatalog clear/empty·예외 경로 TC
- Boundary 81.4% → ≥85%: TableFormatter CAS 연동 또는 TC 보강
- genhtml → build/coverage_html CI 아티팩트

완료 판정:
  lcov --summary build/domain.info   → Line ≥ 95.0%
  lcov --summary build/boundary.info → Line ≥ 85.0%
```

---

## 주의 (프롬프트 해석)

| 항목 | User 요청 | Assistant·문서 처리 |
|------|-----------|---------------------|
| `&&` in shell | cmake && build | PowerShell → **`;`** 또는 별도 명령 |
| ctest | `-V` verbose | MSVC: **`ctest --test-dir build -C Debug`** |
| gcov/lcov | `gcov UnitConverter.cpp` | MSVC 미지원 → **MinGW `build_cov`** 별도 빌드 |
| TC-A-05 | TC-A-01~07 | **미작성** — Dual-Track 13건(6+7)만 검증 |
| json RED | 0 failures | 전체 49건 중 **1건 RED 의도적** — Dual-Track 13/13은 0 failures |
| Domain 95% | lcov extract | **91.9% 미달** — UnitCatalog 미커버 |
| Boundary 85% | lcov extract | **81.4% 미달** — TableFormatter 22.2% |
| 인라인 금지 | src/ + CLI | **`UnitConverter.cpp` 4곳** — M2 CLI 연동 필요 |
| main() 분리 | Domain 위임 | **CLI 레거시** — M2 후속 |
| MinGW 빌드 | (암시) | `InputParser.cpp` `#include <cmath>` 필요 (GCC 15.2) |
| 응답 언어 | (user rule) | 한국어 |

---

## 검증 결과 요약 (본 세션)

### ctest (MSVC)

| 구분 | Passed | Failed |
|------|--------|--------|
| BCE Domain | 28/28 | 0 |
| BCE Data | 7/7 | 0 |
| Dual-Track GREEN | 13/14 | 1 (json RED) |
| **합계** | **48/49** | **1** |

### lcov (MinGW build_cov)

| 레이어 | Line | 목표 | 판정 |
|--------|------|------|------|
| Domain (`src/domain/*`) | 91.9% | ≥ 95% | ❌ |
| Boundary (`src/boundary/*`) | 81.4% | ≥ 85% | ❌ |

### 5항목 체크리스트

| # | 항목 | 판정 |
|---|------|------|
| 1 | TC PASS | 부분 (Dual-Track 13/13 ✅) |
| 2 | Domain ≥ 95% | ❌ 91.9% |
| 3 | Boundary ≥ 85% | ❌ 81.4% |
| 4 | 인라인 상수 없음 | ❌ UnitConverter.cpp |
| 5 | main() Domain 분리 | ❌ CLI 레거시 |

---

## Dual-Track TC ↔ 본 세션 검증 상태

### Track B — `[logic][green]`

| README | Catch2 `TEST_CASE` | 본 세션 |
|--------|-------------------|---------|
| TC-B-01 | `convert_meter_to_feet_returns_correct_ratio` | ✅ PASS |
| TC-B-02 | `convert_meter_to_yard_returns_correct_ratio` | ✅ PASS |
| TC-B-03 | `convert_feet_to_meter_returns_correct_ratio` | ✅ PASS |
| TC-B-04 | `convert_all_returns_all_registered_targets` | ✅ PASS |
| TC-B-05 | `register_unit_enables_conversion` | ✅ PASS |
| TC-B-06 | `load_config_applies_ratios_from_file` | ✅ PASS |
| TC-B-07 | `load_config_invalid_path_keeps_defaults` | ✅ PASS |

### Track A — `[ui][green]` / `[ui][red]`

| README | Catch2 `TEST_CASE` | 본 세션 |
|--------|-------------------|---------|
| TC-A-01 | `valid_input_meter_colon_value_returns_conversion_result` | ✅ PASS |
| TC-A-02 | `input_without_colon_throws_invalid_argument` | ✅ PASS |
| TC-A-03 | `negative_value_throws_invalid_argument` | ✅ PASS |
| TC-A-04 | `unknown_unit_throws_invalid_argument` | ✅ PASS |
| TC-A-05 | *(미작성)* | ⬜ |
| TC-A-06 | `output_preserves_source_unit_and_value` | ✅ PASS |
| TC-A-07 | `zero_value_throws_invalid_argument` | ✅ PASS |
| (확장) | `json_format_returns_valid_schema` | ⬜ RED |

---

## GREEN 검증 명령 (Windows)

### MSVC — ctest

```powershell
cd c:\DEV\UnitConverter_12

# 전체 빌드·테스트
cmake -S . -B build
cmake --build build
ctest --test-dir build -C Debug -V
# 기대: 48/49 Passed (json RED 1건)

# Dual-Track GREEN만
ctest --test-dir build -C Debug -R "green|valid_input|input_without|negative_value|unknown_unit|output_preserves|zero_value|convert_meter|convert_feet|convert_all|register_unit|load_config" --output-on-failure
# 기대: 13/13 Passed

# BCE 회귀
ctest --test-dir build -C Debug -R "domain|data_config" --output-on-failure
# 기대: 35/35 Passed
```

### MinGW — lcov

```powershell
$env:Path = "C:\mingw64\bin;C:\ProgramData\chocolatey\bin;" + $env:Path

cmake -S . -B build_cov -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug -DENABLE_COVERAGE=ON
cmake --build build_cov -j 4
ctest --test-dir build_cov --output-on-failure

lcov --directory build_cov --zerocounters
ctest --test-dir build_cov --output-on-failure
lcov --capture --directory build_cov --output-file build_cov/coverage.info --ignore-errors gcov
lcov --extract build_cov/coverage.info "*/src/domain/*" -o build_cov/domain.info --ignore-errors gcov
lcov --extract build_cov/coverage.info "*/src/boundary/*" -o build_cov/boundary.info --ignore-errors gcov
lcov --summary build_cov/domain.info
lcov --summary build_cov/boundary.info
# 기대(목표): Domain ≥95%, Boundary ≥85% — 현재 미달
```

---

## RPT-12 vs RPT-13 비교

| 항목 | RPT-12 (GREEN) | RPT-13 (본 Export) |
|------|----------------|-------------------|
| Dual-Track TC | 13/14 GREEN | **13/13 재검증 PASS** |
| ctest | 48/49 | **48/49 확인** |
| Domain lcov | 미측정 | **91.9%** (95% 미달) |
| Boundary lcov | 미측정 | **81.4%** (85% 미달) |
| 인라인 상수 | 미검증 | **UnitConverter.cpp 4곳** |
| CLI Domain 분리 | M2 이관 | **미완 확인** |
| InputParser | — | `#include <cmath>` (MinGW) |

---

## 문서 이력

| 버전 | 일자 | 변경 |
|------|------|------|
| 1.0 | 2026-05-21 | 최초 Export (Turn 1~3, 대응 보고서 `13.GREEN_전체검증_커버리지측정.md`) |
